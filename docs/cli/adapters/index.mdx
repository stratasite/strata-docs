# Adapters

Strata supports 8 database adapters for connecting to your data warehouses.

## Supported Adapters

1. **[PostgreSQL](/docs/cli/adapters/postgres)** - Full PostgreSQL support
2. **[Snowflake](/docs/cli/adapters/snowflake)** - Snowflake data warehouse
3. **[MySQL](/docs/cli/adapters/mysql)** - MySQL database
4. **[SQL Server](/docs/cli/adapters/sqlserver)** - Microsoft SQL Server (including Azure)
5. **[Athena](/docs/cli/adapters/athena)** - AWS Athena
6. **[Trino](/docs/cli/adapters/trino)** - Trino/Presto
7. **[DuckDB](/docs/cli/adapters/duckdb)** - Embedded analytics database
8. **[Druid](/docs/cli/adapters/druid)** - Apache Druid

## Common Configuration

All adapters support:

- **adapter** - Adapter name (required)
- **name** - Display name (required)
- **description** - Optional description
- **tier** - Performance tier: `hot`, `warm`, `cold` (default: `warm`)
- **query_timeout** - Query timeout in seconds (default: `3600`)
- **client_name** - Client name sent to database (default: `Strata`)

## Adding Adapters

Use the CLI to add adapters:

```bash
strata datasource add postgres
strata datasource add snowflake
```

Or edit `datasources.yml` directly.

## Authentication

Credentials are stored securely in `.strata` (not committed to git):

```bash
strata datasource auth datasource_key
```

## Next Steps

- Read adapter-specific documentation above
- [Datasources](/docs/semantic-model/datasources) - Tiers and full configuration
- [datasource test](/docs/cli/datasource#test-ds_key) - Test connections
